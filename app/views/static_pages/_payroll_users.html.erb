<tr>
  <td><%= user.full_name %></td>
  <td><%= user.department.name %></td>
  <td>
    <%= user.payroll_hours(@payroll_start, @payroll_end) %>
    <% if user.pay_type == "Salary" %>
      (s)
    <% end %>
  </td>
  <td class="hidden-xs align-right">$ <%= user.payroll_rate %></td>
  <td class="align-right">$ <%= user.payroll_gross(@payroll_start, @payroll_end) %></td>
</tr>